apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: vault-secrets
  namespace: cert-manager
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.local.eryx.pro:8200"
    roleName: "csi-secrets-store-sa"
    objects: |
      - objectName: cloudflare-token
        objectType: secret
        objectPath: secret/cert-manager/cloudflare-token  # Removed 'data/' since it's KV v1